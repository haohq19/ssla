# random
seed: 2025

# model
model_cfg: 'configs/model/MOS_B.yaml'
hot_pixel_thres: 50
conf_thres: 0.001
iou_thres: 0.65

# data
dataset_name: 'Gen1'
window_size: 0.1  # in seconds
num_workers: 4

# training
batch_size: 8
resume_ckpt: 'checkpoints/base.ckpt'